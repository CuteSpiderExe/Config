@model Config.Repository.Models.Zakaz

@{
    ViewBag.Title = "About";
}

<h2>About</h2>

@using (Html.BeginForm("Zakazadd","Zakaz")) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Заказ</h4>
        <hr />
        <div>
            Введите адресс для заказа
        </div>
       <div>
           <input type="text" name="Adres"/>
       </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Заказать" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("К списку заказов", "Index")
</div>
